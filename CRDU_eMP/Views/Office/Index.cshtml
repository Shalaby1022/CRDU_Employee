@model List<CRDU_eMP.Models.Office> // Replace with the correct namespace for your model

<div class="container">
    <h2 class="mb-4">Office List</h2>

    @if (Model.Count > 0)
    {
        <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Location</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var office in Model)
                {
                    <tr class="@(office.Id % 2 == 0 ? "table-primary" : "table-info")">
                        <td>@office.Id</td>
                        <td>@office.Name</td>
                        <td>@office.Location</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Actions">
                                <a asp-action="Details" asp-route-id="@office.Id" class="btn btn-sm btn-info" title="Details"><i class="fas fa-info-circle"></i></a>
                                <a asp-action="Edit" asp-route-id="@office.Id" class="btn btn-sm btn-primary" title="Edit"><i class="fas fa-edit"></i></a>
                                <a asp-action="Delete" asp-route-id="@office.Id" class="btn btn-sm btn-danger" title="Delete"><i class="fas fa-trash-alt"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No offices found.</p>
    }

    <p><a asp-action="Create" class="btn btn-success">Create New Office</a></p>
</div>
